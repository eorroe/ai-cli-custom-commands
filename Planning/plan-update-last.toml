description = "Update Plans to include last update ARGS: <PLAN-FOLDER>"

prompt = """

## Expected Format
The command follows this format: `/plan-update-last <PLAN-FOLDER>`

* "Maintain High Fidelity": "NEVER summarize, paraphrase, or omit existing technical details, code blocks, or specific
constraints. Every line of the original file must be preserved in its full fidelity unless the prompt
explicitly mandate its removal or modification."

## Behavior
1. Explain with user what you understood as their intent for confirmation.
2. Parse and Read the codebase.
3. List out every line of text that will be removed from files so I can confirm whether losing certain lines is okay or must be maintained and avoid loss of content that may not be recovered.
4. Ask for confirmation to proceed with updates.
5. When confirmed to update use the `write_file` tool to update the files in PLAN-FOLDER-NAME to include latest updates of current implementation
6. In detail describe the changes, list what changes were made.

"""
